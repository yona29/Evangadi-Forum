# Production Environment Variables Template
# Copy these variables to your production server's .env file

# Database Configuration (Aiven MySQL)
DB_HOST=your_aiven_host
DB_USER=your_aiven_username
DB_PASS=your_aiven_password
DB_NAME=your_database_name
DB_PORT=3306

# Email Configuration (Gmail)
EMAIL_USER=your_gmail_address@gmail.com
EMAIL_PASS=your_gmail_app_password

# Client URL (Frontend deployment URL)
CLIENT_URL=https://your-frontend-domain.com

# JWT Secret (Generate a strong random string)
JWT_SECRET=your_super_secure_jwt_secret_key_here

# Server Port (Render will set this automatically)
PORT=14255

# AI Configuration (Optional - if using Gemini AI)
GEMINI_API_KEY=your_gemini_api_key
